# Default Alchemy pipeline configuration
# All models default to local MLX inference
# TODO update to latest Unsloth qwen 3.5 models
planner_model:
  provider_type: mlx
  model_id: mlx-community/Qwen2.5-7B-Instruct-4bit

generator_model:
  provider_type: mlx
  model_id: mlx-community/Qwen2.5-7B-Instruct-4bit

validator_model:
  provider_type: mlx
  model_id: mlx-community/Qwen2.5-7B-Instruct-4bit

output_format: huggingface
output_path: ./output
num_samples: 100
batch_size: 10
min_quality_score: 0.7
